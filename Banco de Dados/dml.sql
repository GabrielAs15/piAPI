--CS0:01 CONSULTAR LOGIN
SELECT id_usuario       id,
       ds_email         email,
FROM   tb_login,
WHERE  ds_email       = 'admin@email.com.br'
AND    ds_senha       = '1234'; 



select * from tb_cadastro;

select * from tb_pendencias;
